/ {
    ext_power: ext_power {
        compatible = "zmk,ext-power-generic";
        control-gpios = <&pro_micro 0 GPIO_ACTIVE_HIGH>;
        init-delay-ms = <500>; // Adds 500ms delay after initialization
    };
};
